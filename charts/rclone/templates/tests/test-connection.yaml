apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rclone.fullname" . }}-test-connection"
  labels:
    {{- include "rclone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rclone.fullname" . }}:5572']
  restartPolicy: Never
